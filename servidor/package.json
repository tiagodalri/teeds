{
  "name": "teeds-motor",
  "private": true,
  "type": "module",
  "description": "O motor dos robos da Teeds rodando fora do navegador.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "esbuild src/teste.ts src/servidor.ts --bundle --platform=node --target=node22 --format=esm --outdir=dist --out-extension:.js=.mjs",
    "teste": "node dist/teste.mjs",
    "servidor": "node dist/servidor.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}